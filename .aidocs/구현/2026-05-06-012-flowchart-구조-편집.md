# IMPL-20260506-012 Flowchart 구조 편집

## 관련 요청

- [REQ-20260506-012 Flowchart 구조 편집](../요청/2026-05-06-012-flowchart-구조-편집.md)

## 구현 내용

Flowchart source를 구조적으로 편집하는 제한적 visual editing 기반을 추가했다.

추가/수정:

- `src/diagram/flowchart-structure.ts`
  - `flowchart`/`graph` 감지
  - inline node declaration 파싱
  - rectangle, round, diamond, database, circle, subroutine shape 지원
  - edge 파싱과 label 파싱
  - arrow, line, dotted, thick edge 추가 지원
  - unique node id 생성
  - node 추가, edge 추가, node shape/label 변경
- `src/components/tool-palette/ToolPalette.tsx`
  - Rectangle, Round, Decision, Database, Circle, Subroutine, Edge tool 분리
  - flowchart에서는 shape별 unique node snippet 생성
  - flowchart Edge tool 클릭 시 source snippet 삽입 대신 Structure Edge 설정으로 포커스 이동
  - sequence/class/er/state에서는 diagram-safe snippet 유지
- `src/components/structure/FlowchartStructurePanel.tsx`
  - Flowchart node list
  - 선택 node label/shape 변경
  - source/target/type/label 선택 후 edge 연결
- `src/App.tsx`
  - right rail에 Structure panel 연결
  - Edge tool과 Structure Edge 설정 포커스 연결
  - collaboration 초기 source는 서버 handshake 후 editor extension을 붙이도록 조정하여 중복 Mermaid source merge 방지

## 검증

- `npm run typecheck` 통과
- `npm run test` 통과: 21 files, 66 tests
- `npm run lint` 통과
- `npm run test:e2e` 통과: 11 tests
- `npm run build` 통과

## 남은 확장 후보

- preview SVG 클릭과 source node 선택 연동
- node 삭제와 연결 edge 삭제 옵션
- 기존 edge label/type 변경
- subgraph 생성/편집
